# Realtime Package

Shared contracts for realtime session events will live here.

The package should define:

- Event names.
- Payload schemas.
- Session state model.
- Error codes.
- Versioning rules.

Keeping this contract explicit will make the client and backend easier to evolve independently.
